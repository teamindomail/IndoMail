const mailList=document.querySelector('#mailList');const menuBtn=document.querySelector('#menuBtn');const sidebar=document.querySelector('#sidebar');
const messages=[{sender:'Google',subject:'Security alert',preview:'New sign-in detected on your account.',time:'2:14 AM',color:'#4285f4',unread:true},{sender:'Zoho Team',subject:'Welcome to Zoho Mail',preview:'Your mailbox is ready to use.',time:'9:15 AM',color:'#e14b3e',unread:true},{sender:'IndoMail Team',subject:'Welcome to IndoMail',preview:'Thanks for trying IndoMail.',time:'Yesterday',color:'#6554ea',unread:true},{sender:'GitHub',subject:'Repository activity',preview:'Your project received an update.',time:'Aug 30',color:'#24292f',unread:false}];
function renderInbox(){mailList.innerHTML=messages.map((m,i)=>`<article class="mail ${m.unread?'unread':''}" data-index="${i}"><div class="mail-avatar" style="background:${m.color}">${m.sender[0]}</div><div class="mail-main"><div class="mail-top"><span class="sender">${m.sender}</span><span class="mail-time">${m.time}</span></div><div class="subject">${m.subject}</div><div class="preview">${m.preview}</div></div><button class="star" type="button" aria-label="Star">☆</button></article>`).join('');mailList.querySelectorAll('.mail').forEach(el=>el.addEventListener('click',()=>{const m=messages[Number(el.dataset.index)];alert(`${m.subject}\n\nFrom: ${m.sender}\n\n${m.preview}`)}));}
window.addEventListener('indomail:logged-in',renderInbox);menuBtn?.addEventListener('click',()=>sidebar?.classList.toggle('open'));renderInbox();