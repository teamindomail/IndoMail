const searchBtn=document.querySelector('#searchBtn');
const mailList=document.querySelector('#mailList');
let searchBox=null;
function closeSearch(){searchBox?.remove();searchBox=null}
function runSearch(value){const q=value.trim().toLowerCase();const rows=[...document.querySelectorAll('#mailList .mail')];let shown=0;rows.forEach(row=>{const match=!q||row.textContent.toLowerCase().includes(q);row.style.display=match?'':'none';if(match)shown++});const head=document.querySelector('.panel-head h2');if(head)head.textContent=q?`Search: ${value.trim()}`:'Inbox';let status=document.querySelector('#searchResultStatus');if(!status){status=document.createElement('div');status.id='searchResultStatus';status.className='muted';status.style.padding='12px 10px';mailList?.before(status)}status.textContent=q?`${shown} matching email${shown===1?'':'s'}`:''}
function openSearch(){if(searchBox)return;searchBox=document.createElement('div');searchBox.className='search-bar';searchBox.style.cssText='position:fixed;top:64px;left:50%;transform:translateX(-50%);z-index:100;width:min(620px,calc(100vw - 24px));background:#fff;border:1px solid #d9deea;border-radius:14px;box-shadow:0 12px 35px rgba(15,23,42,.16);padding:10px;display:flex;gap:8px;align-items:center';searchBox.innerHTML='<input id="mailSearchInput" type="search" placeholder="Search emails…" autocomplete="off" style="flex:1;border:0;outline:0;font-size:15px;padding:8px 10px"><button type="button" id="mailSearchClose" class="detail-btn" aria-label="Close search">×</button>';document.body.appendChild(searchBox);const input=searchBox.querySelector('#mailSearchInput');input.addEventListener('input',()=>runSearch(input.value));searchBox.querySelector('#mailSearchClose').addEventListener('click',()=>{closeSearch();runSearch('')});input.focus();}
searchBtn?.addEventListener('click',openSearch);document.addEventListener('keydown',e=>{if(e.key==='Escape'&&searchBox){closeSearch();runSearch('')}});
